# 100hires-portfolio-project
Portfolio project for 100Hires application process

## Tools Installed

- **Cursor IDE** — Downloaded and installed from cursor.com
- **Claude Code** — Installed as an extension inside Cursor (Extensions panel, Ctrl+Shift+X)
- **Codex** — Installed as an extension inside Cursor (Extensions panel, Ctrl+Shift+X)

## Steps Completed

1. Downloaded and installed Cursor IDE from cursor.com
2. Opened the Extensions panel in Cursor (Ctrl+Shift+X)
3. Searched for and installed the Claude Code extension, then logged in
4. Searched for and installed the Codex extension, then logged in
5. Created a public GitHub repository named `100hires-portfolio-project`
6. Created this README.md file documenting the process

## Issues Encountered

- At first I navigated to the Cursor web dashboard instead of opening the desktop application. I realized the extensions panel only exists inside the installed app, not the website, and solved it by opening Cursor directly from my desktop.
- Finding the extensions panel wasn't immediately obvious — I used Ctrl+Shift+X which opened it directly.

## Notes

This is the first step of the 100Hires portfolio project process. Ready for the next stage.
